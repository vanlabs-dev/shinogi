# public-pulse

Session 4 ship. 2026-09-02.

## What shipped

Pre-Atlas public-pulse shell. Not an edition.

- `index.html`: five landmarks (`#network` `#movers` `#mining`
  `#attention` `#code-narrative`), named gaps, as-of line
  `awaiting first Atlas publish`. Same dark mono chrome.
- `404.html`: unchanged miss page.
- `tests/test_page_contract.py`: local contract test. `pytest -q` PASS.
- Living spec: `openspec/specs/public-pulse/`.

## Artifacts

- Change: `openspec/changes/archive/2026-09-02-public-pulse/`
- Session notes: `docs/greenfield/public-pulse/`
- Verify: `08-verify.md` RESULT: PASS

## Follow-ups

Atlas renderer on the fleet unit: compose from briefing stores, keep
the landmark ids, push on hash change. Not this repo.
